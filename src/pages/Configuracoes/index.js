import { View, Text } from "react-native";
import { PagesStyles } from "../../styles/PagesStyles";

export default function Configuracoes() {
    return(
        <View style={PagesStyles.pages}>
            <Text style={PagesStyles.label}>Tela de CONFIGURAÇÕES</Text>
        </View>
    )
}